{
  "name": "progressive-app-test",
  "version": "1.0.0",
  "description": "Test Progressive ap",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --ss 'static/' -port 8080   --files 'static/*.html, static/css/*.css, static/scripts/*.js, static/*.json' --server"
  },
  "author": "Jason Dalton",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.12.10"
  }
}
